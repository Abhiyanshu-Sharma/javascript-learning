// "use strict";
// Variables

// Variables can stores values/information
// we can use that later 
// we can also change that info/value later

// declare a variable
var firstName = 'Abhiyanshu';

// Can be also made like this but this is risky
// To avoid this we write "use strict" it gives error
lastName = ' Sharma';

// Use a variable
console.log('Before changing the value: ' + firstName + lastName);

// change its value
firstName = 'Abhi';

console.log('After changing the value: ' + firstName + lastName);